<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:keepScreenOn="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/main_txt_top"
        android:gravity="center"
        android:text="Wonky Tonki!"
        android:textSize="40sp"
        android:layout_width="match_parent"
        android:layout_height="60dp" />
    
    <Button
        android:background="@drawable/button_background"
        android:layout_width="280dp"
        android:layout_height="280dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/main_btn_talk"
        android:layout_below="@+id/main_txt_top"/>

    <TextView
        android:id="@+id/main_txt_bottom"
        android:text="Connecting..."
        android:textSize="40sp"
        android:gravity="center"
        android:layout_below="@id/main_btn_talk"
        android:layout_above="@+id/main_btn_reconnect"
        android:layout_width="match_parent"
        android:layout_height="100dp" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Force Reconnect!"
        android:id="@+id/main_btn_reconnect"
        android:layout_alignParentBottom="true" />
</RelativeLayout>
