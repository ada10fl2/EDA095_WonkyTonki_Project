apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

mainClassName = "com.wonkytonki.server.Main"

repositories {
    mavenCentral()
    maven {
        url "http://clojars.org/repo/"
    }
}

jar {
    baseName = 'test'
    version =  '0.0.1'
}

dependencies {
    compile "org.clojars.miguno:kryonet-all:2.+"
    testCompile "junit:junit:+"
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
